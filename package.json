{
  "scripts": {
    "build": "npm install -g postcss postcss-cli autoprefixer && hugo --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "postcss": "^8.4.6",
    "postcss-cli": "^9.1.0"
  }
}
